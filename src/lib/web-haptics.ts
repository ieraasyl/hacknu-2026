import type { WebHapticsOptions } from 'web-haptics';

export const webHapticsOptions: WebHapticsOptions = {
  debug: false, // change to true for development
  showSwitch: false, // you can change this to true as well for development if you want to see the haptic toggle
};
